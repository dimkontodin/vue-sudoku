{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The page never calls Tauri APIs, so it gets none.",
  "windows": [
    "main"
  ],
  "permissions": []
}
